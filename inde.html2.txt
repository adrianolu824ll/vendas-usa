<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Vendas — Profissional | Verde & Preto</title>
  <meta name="description" content="Site profissional de vendas com design verde e preto. Clique para visitar nosso Instagram @__shadowthornn." />
  <meta property="og:title" content="Vendas — Profissional | Verde & Preto" />
  <meta property="og:description" content="Site profissional de vendas. Siga no Instagram: @__shadowthornn." />
  <meta property="og:type" content="website" />
  <meta property="og:locale" content="pt_BR" />
  <link rel="icon" href='data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><text y="0.9em" font-size="90">💲</text></svg>' />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0b0f12;           /* preto profundo */
      --bg-2:#0f1419;         /* preto suave */
      --green:#00e676;        /* verde vivo */
      --green-2:#00c853;      /* verde escuro */
      --text:#e6eef5;         /* texto claro */
      --muted:#9bb0c0;        /* texto secundário */
      --card:#10161c;         /* cards */
      --border:#1b242d;       /* bordas */
      --shadow: 0 10px 30px rgba(0,0,0,.35), 0 2px 8px rgba(0,0,0,.2);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family:Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color:var(--text); background: radial-gradient(1200px 600px at 10% -10%, rgba(0,230,118,.12), transparent 60%),
                         radial-gradient(1000px 500px at 110% 20%, rgba(0,200,83,.10), transparent 60%),
                         var(--bg);
      overflow-x:hidden;
    }
    .container{max-width:1200px; margin-inline:auto; padding:24px}
    header{
      position:sticky; top:0; z-index:50; backdrop-filter:saturate(120%) blur(8px);
      background:linear-gradient(180deg, rgba(11,15,18,.9), rgba(11,15,18,.6));
      border-bottom:1px solid var(--border);
    }
    .nav{display:flex; align-items:center; justify-content:space-between; gap:16px}
    .brand{display:flex; align-items:center; gap:12px; text-decoration:none}
    .logo{
      width:40px; height:40px; border-radius:12px; display:grid; place-items:center;
      background:linear-gradient(135deg, var(--green), var(--green-2));
      color:#0a0a0a; font-weight:900; box-shadow:var(--shadow);
    }
    .brand h1{font-size:18px; margin:0; letter-spacing:.4px}
    .brand small{display:block; color:var(--muted); font-weight:400}
    .nav-actions{display:flex; align-items:center; gap:12px}
    .btn, .btn-outline{
      cursor:pointer; border:none; border-radius:14px; padding:12px 18px; font-weight:700;
      transition:transform .08s ease, box-shadow .2s ease, background .2s ease;
    }
    .btn{ background:linear-gradient(135deg, var(--green), var(--green-2)); color:#091012; box-shadow:var(--shadow) }
    .btn:hover{ transform:translateY(-1px) }
    .btn:active{ transform:translateY(0) scale(.98) }
    .btn-outline{
      background:transparent; color:var(--text); border:1px solid var(--border)
    }
    .btn-outline:hover{ border-color:var(--green); color:var(--green) }

    /* HERO */
    .hero{ position:relative; padding:72px 0 48px; overflow:hidden }
    .hero-grid{ display:grid; grid-template-columns:1.2fr 1fr; gap:28px; align-items:center }
    .eyebrow{ color:var(--green); font-weight:800; letter-spacing:.12em; text-transform:uppercase; font-size:12px }
    .title{ font-size: clamp(28px, 3.2vw + 10px, 56px); line-height:1.06; margin:8px 0 12px; font-weight:800 }
    .subtitle{ color:var(--muted); font-size: clamp(14px, 1.1vw + 8px, 18px); max-width:60ch }
    .cta-row{ display:flex; gap:12px; margin-top:22px; flex-wrap:wrap }

    /* Dollar rain */
    .money-bg{ position:absolute; inset:-20% -10%; pointer-events:none; filter: drop-shadow(0 8px 18px rgba(0,0,0,.45)); opacity:.25 }
    .money-bg span{
      position:absolute; font-weight:900; font-size: clamp(16px, 2.2vw, 34px);
      color: var(--green);
      transform: translateY(0) rotate(0deg);
      animation: floatUp linear infinite;
      user-select:none;
    }
    @keyframes floatUp{
      from{ transform: translateY(40vh) rotate(0deg); opacity: 0 }
      10%{ opacity: .8 }
      to{ transform: translateY(-80vh) rotate(360deg); opacity: 0 }
    }

    /* SEÇÃO PRODUTOS */
    .section{ padding:42px 0 }
    .section-header{ display:flex; align-items:end; justify-content:space-between; gap:16px; margin-bottom:18px }
    .section-title{ font-size:22px; font-weight:800 }
    .grid{ display:grid; grid-template-columns: repeat(3, 1fr); gap:18px }
    .card{
      background:linear-gradient(180deg, var(--card), rgba(16,22,28,.8));
      border:1px solid var(--border); border-radius:20px; padding:16px; box-shadow:var(--shadow);
      display:flex; flex-direction:column; gap:10px
    }
    .pill{ display:inline-flex; align-items:center; gap:8px; padding:6px 10px; border-radius:999px; background:#0d1319; border:1px solid var(--border); color:var(--muted); font-size:12px }
    .price{ font-weight:800; font-size:22px; color:var(--green) }
    .card .btn{ align-self:flex-start }

    /* FOOTER */
    footer{ border-top:1px solid var(--border); margin-top:36px; background:var(--bg-2) }
    .footer-inner{ display:flex; flex-wrap:wrap; gap:16px; align-items:center; justify-content:space-between; padding:20px 0 }
    .copy{ color:var(--muted); font-size:14px }

    /* RESPONSIVO */
    @media (max-width: 960px){ .hero-grid{ grid-template-columns:1fr } .money-bg{ opacity:.2 } }
    @media (max-width: 720px){ .grid{ grid-template-columns:1fr 1fr } }
    @media (max-width: 520px){ .grid{ grid-template-columns:1fr } }
      /* BOTÃO FLUTUANTE INSTAGRAM */
    .fab-instagram{
      position:fixed; right:18px; bottom:18px; z-index:60; border:none; border-radius:999px;
      padding:14px 18px; font-weight:800; background:linear-gradient(135deg, var(--green), var(--green-2));
      color:#091012; box-shadow: var(--shadow); text-decoration:none; display:inline-flex; align-items:center; gap:10px;
    }
    .fab-instagram:hover{ transform: translateY(-1px); }
    .fab-instagram:active{ transform: translateY(0) scale(.98); }
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <a class="brand" href="#top" aria-label="Página inicial">
        <div class="logo" aria-hidden="true">$</div>
        <div>
          <h1>Vendas Profissionais</h1>
          <small>Verde & Preto • Qualidade e Confiança</small>
        </div>
      </a>
      <div class="nav-actions">
        <button class="btn-outline" id="btnTema" aria-label="Alternar tema">Tema</button>
        <a class="btn" id="btnInstagramHeader" href="https://www.instagram.com/__shadowthornn/" target="_blank" rel="noopener noreferrer">Ir ao Instagram</a>
      </div>
    </div>
  </header>

  <main>
    <section class="hero">
      <div class="money-bg" aria-hidden="true" id="moneyBg"></div>
      <div class="container hero-grid">
        <div>
          <div class="eyebrow">$ Ofertas que valorizam seu dinheiro</div>
          <h2 class="title">Seu hub <span style="color:var(--green)">profissional</span> de vendas</h2>
          <p class="subtitle">Apresentação elegante em verde e preto, foco em conversão e confiança. Clique no botão abaixo para falar e acompanhar novidades no Instagram.</p>
          <div class="cta-row">
            <a class="btn" id="btnInstagramHero" href="https://www.instagram.com/__shadowthornn/" target="_blank" rel="noopener noreferrer">Seguir no Instagram</a>
            <a class="btn-outline" href="#produtos">Ver produtos</a>
          </div>
        </div>
        <div>
          <div class="card" style="min-height:220px">
            <span class="pill">💹 Destaque</span>
            <h3 style="margin:6px 0 2px">Design focado em vendas</h3>
            <p style="color:var(--muted); margin:0">Layout otimizado com CTA claros, contraste alto e símbolos de <strong>$</strong> animados para dar identidade financeira ao seu projeto.</p>
            <ul style="margin:8px 0 0 18px; color:var(--muted)">
              <li>Verde & preto profissionais</li>
              <li>Botão direto para Instagram</li>
              <li>Cards de produto prontos</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <section id="produtos" class="section">
      <div class="container">
        <div class="section-header">
          <h3 class="section-title">Produtos em destaque</h3>
          <a class="btn-outline" href="#contato">Precisa de algo específico?</a>
        </div>
        <div class="grid">
          <article class="card">
            <span class="pill">Novo</span>
            <h4 style="margin:4px 0 0">Produto 1</h4>
            <p style="color:var(--muted); margin:8px 0 4px">Descrição breve do produto para vender o valor.</p>
            <div class="price">R$ 99,90</div>
            <button class="btn">Comprar</button>
          </article>
          <article class="card">
            <span class="pill">Top</span>
            <h4 style="margin:4px 0 0">Produto 2</h4>
            <p style="color:var(--muted); margin:8px 0 4px">Descrição breve do produto para vender o valor.</p>
            <div class="price">R$ 149,90</div>
            <button class="btn">Comprar</button>
          </article>
          <article class="card">
            <span class="pill">Promo</span>
            <h4 style="margin:4px 0 0">Produto 3</h4>
            <p style="color:var(--muted); margin:8px 0 4px">Descrição breve do produto para vender o valor.</p>
            <div class="price">R$ 79,90</div>
            <button class="btn">Comprar</button>
          </article>
        </div>
      </div>
    </section>

    <section id="contato" class="section" aria-label="Contato e redes sociais">
      <div class="container">
        <div class="card">
          <h3 style="margin:6px 0 8px">Vamos conversar?</h3>
          <p style="color:var(--muted); margin:0 0 12px">Clique no botão abaixo para ir direto ao Instagram e falar comigo.</p>
          <a class="btn" id="btnInstagramContato" href="https://www.instagram.com/__shadowthornn/" target="_blank" rel="noopener noreferrer">Ir para o Instagram</a>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container footer-inner">
      <div class="brand" style="text-decoration:none">
        <div class="logo" aria-hidden="true">$</div>
        <div>
          <strong>Vendas Profissionais</strong>
          <div style="color:var(--muted); font-size:12px">Feito com foco em conversão</div>
        </div>
      </div>
      <div class="copy">© <span id="year"></span> • Todos os direitos reservados.</div>
    </div>
  </footer>

  <!-- Botão flutuante para Instagram -->
  <a class="fab-instagram" href="https://www.instagram.com/__shadowthornn/" target="_blank" rel="noopener noreferrer" aria-label="Abrir Instagram @__shadowthornn">
    <span>Instagram</span>
  </a>
  <script>
    // Ano atual
    document.getElementById('year').textContent = new Date().getFullYear();

    // Geração de símbolos $ animados
    const moneyBg = document.getElementById('moneyBg');
    const TOTAL = 36; // quantidade de símbolos
    for(let i=0;i<TOTAL;i++){
      const s = document.createElement('span');
      s.textContent = '$';
      const size = 0.8 + Math.random()*1.6; // escala
      const left = Math.random()*100;       // posição X
      const delay = Math.random()*8;        // atraso
      const dur = 8 + Math.random()*10;     // duração
      s.style.left = left+'%';
      s.style.top = (20 + Math.random()*60)+'%';
      s.style.fontSize = `calc(16px * ${size})`;
      s.style.animationDuration = dur+'s';
      s.style.animationDelay = delay+'s';
      s.style.opacity = 0.6 + Math.random()*0.3;
      moneyBg.appendChild(s);
    }

    // Simples alternador de tema (ajusta saturação do fundo)
    const btnTema = document.getElementById('btnTema');
    let vivid = true;
    btnTema.addEventListener('click', ()=>{
      document.body.style.background = vivid ? 'var(--bg)' : 'radial-gradient(1200px 600px at 10% -10%, rgba(0,230,118,.12), transparent 60%), radial-gradient(1000px 500px at 110% 20%, rgba(0,200,83,.10), transparent 60%), var(--bg)';
      vivid = !vivid;
    });

    // Rastreamento simples de cliques no Instagram
    const instaButtons = ['btnInstagramHeader','btnInstagramHero','btnInstagramContato']
      .map(id => document.getElementById(id));

    instaButtons.forEach(btn => btn && btn.addEventListener('click',()=>{
      try {
        localStorage.setItem('lastInstagramClick', new Date().toISOString());
        console.log('Instagram clicado em:', new Date().toLocaleString());
      } catch(e) {}
    }));
  </script>
</body>
</html>

